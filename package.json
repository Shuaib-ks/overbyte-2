{
  "name": "overbyte",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Persistent business inventory and surplus marketplace",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "libsql": "0.5.29"
  }
}
